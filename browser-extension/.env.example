# API 基础 URL（可选，用于后端服务）
VITE_API_BASE_URL=http://localhost:8000/api/v1

# 统计服务 URL（可选，用于收集匿名使用数据）
# 部署 analytics-service 后填写实际的 Worker URL
VITE_ANALYTICS_URL=https://your-worker.your-account.workers.dev/event