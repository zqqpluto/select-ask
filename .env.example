# Select Ask 浏览器插件环境变量配置

## 说明

Select Ask 是一个完全本地化的浏览器插件，不需要任何后端服务。
所有数据（包括 API Key）都存储在用户的浏览器本地，直接调用第三方 LLM API。

## 环境变量

当前没有需要配置的环境变量。

## 开发说明

- 所有模型配置在插件的 Options 页面进行
- API Key 使用 AES-256-GCM 加密后存储在 chrome.storage.sync
- 支持的 LLM 提供商：OpenAI、Anthropic、DeepSeek、通义千问、智谱 GLM、自定义 OpenAI 兼容 API